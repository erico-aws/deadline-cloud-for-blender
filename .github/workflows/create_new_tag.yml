name: "Create new Tag"

on:
  push:
    branches:
      - mainline
    paths:
      - CHANGELOG.md

concurrency:
  group: release

jobs:
    create-new-tag:
        uses: erico-aws/.github/.github/workflows/reusable_create_new_tag.yml@mainline
        secrets: inherit